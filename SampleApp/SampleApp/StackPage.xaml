<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Padding="20"
             x:Class="SampleApp.StackPage">
    <StackLayout>
        <Switch x:Name="switch" IsToggled="False" Toggled="Switch_Toggled"></Switch>
        <Label x:Name="label" Text="Text 1"></Label>
        <Label Text="Text 2" IsVisible="{Binding Source={x:Reference switch}, Path=IsToggled}"></Label>
        <Slider x:Name="slider" Value="100" Minimum="0" Maximum="255"></Slider>
        <Label Text="{Binding Source={x:Reference slider}, Path=Value, StringFormat='{0:N0}'}"></Label>
        <Stepper x:Name="stepper"></Stepper>
        <Label Text="{Binding Source={x:Reference stepper}, Path=Value, StringFormat='{0:N0}'}"></Label>
        <Entry Keyboard="Numeric" Placeholder="Phone" Completed="Entry_Completed"></Entry>
        <Label x:Name="completedLabel"></Label>
        <Entry x:Name="password" IsPassword="True"></Entry>
        <Label Text="{Binding Source={x:Reference password}, Path=Text}"></Label>
        <Picker x:Name="contactMethods" Title="Contact method" SelectedIndexChanged="Picker_SelectedIndexChanged">
        </Picker>
    </StackLayout>
</ContentPage>